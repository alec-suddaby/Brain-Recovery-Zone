using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttentionTrainingPBCount : MonoBehaviour
{
    public string timerSaveTest = "00:00";
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }




    // Detect if the scene is one of the scenes to register (possibly by a local save script)
    // Collect timer data from the local scene script
    // Save that into a string
    // Rank timings
    // Write data to local txt file

    // Clear the strings and local txt file
    
    // Flag if something is with audio

}
